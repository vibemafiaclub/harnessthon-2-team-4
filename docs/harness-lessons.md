# harness-lessons — 재현된 실패 → 범용 규칙 → 어디에 반영했나

> 형식: 실패(무엇이 실제로 일어났나) / 원인 / 범용 규칙 / 반영 위치. 프로젝트 고유 값은 적지 않는다.
> 새 항목은 아래에 append 한다. 날짜는 절대 날짜.

## 2026-09-05 — 청첩장모임 스케줄러 1차 제작·검증에서

| # | 실패 | 원인 | 범용 규칙 | 반영 |
|---|---|---|---|---|
| L1 | 사용자가 URL을 주지 않아 새 파일에 제작 → 도중에 사용자가 다른 파일을 지정 → 두 파일에 나뉘어 존재 | G-3에서 "새 파일 생성"을 사용자에게 즉시 보이지 않았음 | 에이전트가 만든 target은 첫 보고에 URL을 보여 주고 가정 로그 "높음"으로 남긴다. target 변경 시 이전 파일은 backup, 제출본 결정은 미해결 결정 이름으로 | SKILL G-3 |
| L2 | 같은 파일에 두 세션이 같은 스크립트를 병렬 실행 → 컴포넌트 중복 생성, 하나는 크기 버그 | 파일 쓰기 소유권 미확인 | 같은 파일에 쓰는 세션은 하나. 내가 만들지 않은 노드가 보이면 쓰기를 멈추고 소유자를 묻는다 | SKILL G-3 |
| L3 | 전용 스킬에 file key·노드 ID 상수를 적어 둠 → 원장과 어긋남 | 값이 코어/전용 스킬에 복제됨 | target 원장은 brief.md 하나. 전용 스킬·project.json에 복제 금지. 검사기가 상수를 잡는다 | SKILL 프로젝트 설정, wedding 스킬 §1, check_contracts #2 |
| L4 | `use_figma` 반환 20KB 초과로 노드 트리 스냅샷이 잘림 | 출력 제한 | 스냅샷을 "사실 파일(위반 목록·카운트)" + "분할 원본 트리" 두 층으로. 수집 범위와 계산 방법을 파일에 명시 | SKILL 스냅샷 계약 |
| L5 | 대비율 위반 61건 — 소프트 배경 위 12~13px 라벨과 3차 텍스트 | 토큰 선정 시 대비율을 계산하지 않음 | status/side 진한 색은 토큰 확정 전에 소프트 배경과 대비율 계산. 비활성 컨트롤 라벨은 면제 표기 | SKILL 제작단계 함정, A-0 |
| L6 | 상태 탭 칩 5개 중 1개가 프레임 밖으로 넘침 | 가로 스크롤 선언 없이 한 줄 배치 | 스크롤/오버레이는 brief `layout.scroll_containers`에 선언. 선언 없으면 FAIL | SKILL A-0, brief 템플릿 |
| L7 | `FRAME.description` 설정 시 TypeError → 화면 2개 제작 호출 롤백 | Plugin API: description은 COMPONENT만 | 화면 설명은 프레임 아래 notes 텍스트 노드 | SKILL 제작단계 함정 |
| L8 | `resize()` 후 컴포넌트 높이가 10px로 고정 | resize가 hug를 FIXED로 되돌림 | 고정 폭 컴포넌트는 resize 뒤 반대 축을 AUTO로 재설정 | SKILL 제작단계 함정 |
| L9 | `combineAsVariants` 실패로 컴포넌트 승격 + 화면 치환 한 호출 전체 롤백 | 부모 페이지 불일치 + 한 호출에 두 작업 | 컴포넌트 생성과 화면 치환은 별도 호출. 원본 지우기 전 텍스트 복사 | SKILL 제작단계 함정 |
| L10 | 화면 content가 844px 프레임에서 잘림 (여러 화면) | 고정 높이 프레임 + 항목 과다 | 화면 스크립트 끝에 높이 합 검사, 넘치면 항목·gap 조정 | SKILL 제작단계 함정 |
| L11 | 아바타 겹침 gap -10/-6이 A-3 off-grid로 걸림 | 음수 gap을 그리드 밖으로 둠 | 음수 gap도 배수(-8) | SKILL 제작단계 함정 |
| L12 | A-4 재사용률 41%→45%, 하한 70% 미달 | 산식(REUSE-A)이 레이아웃 래퍼·본문 텍스트를 분모에 포함하는데 하한은 산식 확인 없이 정해짐 | 하한은 산식 ID와 함께 승인. 산식 바꾸면 다른 ID·다른 하한. 컴포넌트별 인스턴스 수·남은 반복 프레임·깊이를 함께 보고 | SKILL A-4, brief 템플릿 thresholds |
| L13 | "신랑/신부/양가를 항상 색으로 구분"이 `사람` 소스로 확정 처리 | PRD 원문과 에이전트 해석을 같은 소스로 묶음 | 소스 5종(`사람-원문`/`사람-해석`/`실측`/`가정(위임)`/`차단`). 해석은 항상 후보 | SKILL 0단계 소스 어휘, brief 템플릿 |
| L14 | C 검증자가 화면 간 더미 인물 불일치·카운트 불일치를 "참고 관찰"로만 남김 → 판정에 미반영 | 판단기준 밖 관찰의 라우팅 규칙 없음 | 참고 관찰은 원문 보존 → 데이터 정합 수정으로 라우팅(V-5) | SKILL 검증 배선 ⑥, V단계 |
| L15 | A/C를 통과해도 "요구가 화면에 실제로 있는가·CTA가 이어지는가"는 아무도 확인하지 않음 | 검증 단계 누락 | V단계(요구·까다로운 경우·흐름·상태·데이터 정합·리소스 실재) 추가 | SKILL V단계, decisions 템플릿 |
| L16 | 반복 구조(섹션 헤더·타일·행)를 화면마다 프레임으로 다시 그림 → 나중에 승격하며 크래시 | 제작-L/S 분리 미비 | 같은 구조 2회 반복 시 그 자리에서 컴포넌트 승격 | SKILL 제작 순서 |
| L17 | 검증 Agent가 조직 월 한도(429)로 중단 → 판정 없음 | 한도 | BLOCKED로 기록, 자동 재시도 금지, 제작자 자체 계산으로 대체 금지. figma-mcp.md의 "지수 백오프 재시도" 문구가 AGENTS 계약과 충돌 → 삭제 | SKILL G-4·A-라우팅, figma-mcp.md, GATE status |
| L18 | 컴포넌트 이름 규약과 brief의 required_states 이름(`StatusBadge` vs `Badge/Status`)이 달라 검증자가 의미 매핑으로 대응 | 원장과 실측의 이름 불일치 | required_states는 실제 컴포넌트 세트 이름으로 적는다. 검사기 후속 항목 | brief(청첩장) 정정 대상 |
| L19 | 0바이트 PRD 파일이 정상 입력처럼 존재 | 파일 이름만으로 판단 | PRD 최소 길이 검사 | check_contracts #1 |
| L | 하이파이 화면을 바로 만들어 검증까지 갔지만, 기능이 사용자 기대와 맞는지는 아무도 확인하지 않았다(검증자 관찰: 데이터 정합 불일치는 잡았지만 "이 기능이 필요한가"는 범위 밖) | 프로세스에 사용자 피드백 지점이 없음 | 3막: W 와이어프레임(토큰 금지) → I 기능 인터뷰(시나리오 과제, 원문 기록, 승격은 사람) → U UI 상세(A/C/V). I 없이 U 금지(생략은 사람 결정) | SKILL 디자인 프로세스 3막, templates/interview.md, project.json `interview` |
| L21 | 검사기를 만들자마자 코어(SKILL.md)의 "실제 사례" 문장에서 도메인 용어 누출이 잡힘 | 교훈을 적으면서 프로젝트 이름을 그대로 씀 | 코어의 사례는 일반화해 쓰고 원문은 harness-lessons에 둔다. 다른 프로젝트 언급은 project-id로 | check_contracts #4·#5 |
| L | (디자이너 피드백) 클라이언트가 "좋다"고 한 뒤 UX를 크게 바꾸자고 하는 상황을 피하고 싶다 | 단계별 확정이 기록되지 않아 뒤 막에서 앞 막을 흔듦 | 막별 LOCK 블록. 잠긴 항목은 UNLOCK 요청(사람 승인, 라운드 +1)으로만 변경 | SKILL 4막·LOCK, decisions 템플릿 |
| L23 | (디자이너 피드백) "따뜻해 보여요"를 그대로 기준으로 쓰면 틀린다 | 첫 반응은 라벨 | 되묻기 규칙: 왜? + 같은 축 유사 변형 2~3개로 반응이 꺾이는 지점 찾기. probe 없는 후보는 승격 금지 | SKILL 되묻기 규칙, interview 템플릿 probe 열 |
| L24 | (디자이너 피드백) 무드를 먼저 잡고, 와이어프레임은 HTML 로우파이로 중간 컨펌 후 Figma | 3막에 무드 단계·중간 컨펌이 없었음 | 4막(M→W→I→U). W는 HTML → 컨펌 → Figma 2단. HTML은 검증 매체(예외 명시) | SKILL 4막 |
| L25 | 인터뷰 스킬 이름이 "ooo"로만 전달돼 후보를 잘못 짚음(appbuild-plan-interview) | 외부 도구 참조를 경로로 받기 전 추측 | 외부 스킬은 경로·버전을 실측한 뒤 어댑터 문서(개념 매핑·실행 경로·금지)를 만들고 project.json에 연결 | docs/integrations/ooo-interview.md |
| L26 | 온보딩 없이 시작 → target 임의 생성(L1), 막 순서 뒤집힘(L20), 스킬 이름 추측(L25)이 한 세션에서 연쇄 | 워크플로 앞에 "누가·무엇으로·어디서부터"를 잠그는 단계가 없음 | O단계 온보딩(역할표·설정·막 상태표·대상 파일·도구 실측·자산·약속·재진술). 질문지 아니라 재진술로 끝낸다 | SKILL O단계, templates/onboarding.md |

## 회귀 fixture 아이디어 (미구현)

- 전용 스킬에 file key 삽입 → check_contracts ERROR (L3) — **구현됨**
- brief.md target ≠ _system target → ERROR (L1) — **구현됨**
- 코어에 `groom` 삽입 → ERROR (도메인 누출) — **구현됨**
- GATE status=PASS + output_refs 빈 배열 → ERROR — **구현됨**
- 잘린 JSON 스냅샷 → A BLOCKED (L4) — 미구현
- 렌더 revision ≠ 현재 revision → C BLOCKED — 미구현

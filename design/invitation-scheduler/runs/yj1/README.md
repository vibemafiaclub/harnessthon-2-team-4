# runs/yj1 — feat/yj1 브랜치의 청첩장모임 스케줄러 실행 (보존본)

> 합병 2026-09-05. `origin/feat/yj1` `projects/cheongchup-scheduler/*`를 **무수정 보존**했다. 이 run은 dev run(`../brief.md`, `../decisions.md`)과 **다른 Figma 파일·다른 팔레트**에서 진행됐다.

| 항목 | 값 |
|---|---|
| Figma 파일 | https://www.figma.com/design/dqTi2Vo9EDHvTFmackbRQ0/ (사용자 제공 2026-09-05; yj1 문서에는 미기록) |
| 팔레트 | 로즈 `#B84A6B` 액센트 — yj1 brief에 "사람 지정"으로 기록. 되묻기(probe) 기록 없음 → dev 원장에서는 `사람-원문` 후보 |
| 화면 | 홈·지인관리·모임편성·모임상세·전체일정·일정후보제안·받은요청·지인추가·확정완료·빈상태·삭제확인 + 온보딩 3화면(역할선택·내정보·파트너 초대) |
| 검증 이력 | `verify-log.md` 15건 (A 6·C 9). 홈 C 3회로 상한 도달 |
| LOCK | 없음 (yj1에는 LOCK 체계가 없었다) |
| 파일 대응 | `criteria.md` → dev `brief.md` A단계 기준값 + `contracts/requirements.json` / `verify-log.md` → dev `decisions.md` 검증 로그 / `build-plan.md` → dev 화면 요소 목록 |

**dev run과 합치는 방법**: 이 run의 결정을 dev로 옮길 때는 `sources`를 그대로 인용한다(예: "yj1 references.md 캘링 — 봤는가=아니오"). 팔레트·화면 구조 차이는 M막·W막에서 **두 레퍼런스로 나란히** 제시해 사람이 되묻기로 고른다. 자동 병합하지 않는다.

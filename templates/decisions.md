<!--
B단계(발산·수렴) 산출물 템플릿. 산출물 루트로 복사해서 채운다.
빈 템플릿입니다 — 아래는 전부 TODO/형식 예시이며 실제 내용이 아닙니다.

★ 이 파일은 append 전용이다. 새 라운드는 아래에 덧붙이고, 이전 라운드를 덮어쓰지 않는다.
  구현할 때 Write로 파일 전체를 다시 쓰지 말고 Edit로 끝에 삽입한다.
  이전 라운드를 지우면 반복실패 판정 근거와 재시도 카운터가 동시에 사라진다.
★ 이 파일은 C단계 검증 Agent에게 주지 않는다 (제작 의도를 알면 자기변호를 한다).
-->

# Decisions

## 라운드 로그

> C-라우팅이 이 표를 읽는다. **"C 탈락 항목" 열을 라운드 간 비교해** 반복 실패를 판정한다.
> 라운드 수(N)와 하위 라운드 수(N.k)가 그대로 재시도 카운터다. 기본 상한 각 3.
> 비워 두면 무한루프 방지가 작동하지 않는다.

| 라운드 | 모드 | 시작 사유 | baseline | C 탈락 항목 | 라우팅 판정 | 하위 라운드 최대 |
|---|---|---|---|---|---|---|
| 1 | TODO (plan-only/build/review/improve) | 최초 발산 | TODO | TODO | TODO | 1.0 |

---

## 라운드 1

### 시작 사유

TODO: `최초 발산` — 또는 재발산이면 `라운드 N에서 [항목] 탈락 → 방향 오류 판정으로 회귀`

### 식별된 독립 축과 baseline

> 축을 나누지 않고 화면 전체를 통째로 비교하면, 상대가 무엇 때문에 골랐는지 뒤섞여 판단 근거가 안 남는다.
> **독립성 검사**: 한 축의 값을 바꿔도 다른 축이 따라 바뀌지 않아야 한다. 따라 바뀌면 한 축이다 — 합친다.
>
> ★ **baseline이 반드시 필요하다.** 비교 축 외의 축을 고정해 주지 않으면 각 후보 Agent가 임의로
>   고르고 그 순간 축이 오염된다. 눈가림은 앵커링을 막지만 미지정 축의 임의 선택은 막지 못한다.

| 축 | 이 라운드에서 비교하는가 | baseline 값 (비교 안 하는 축은 이 값으로 고정) |
|---|---|---|
| TODO | 예 | — |
| TODO | 아니오 | TODO |

### 축별 후보·비평·선택

> 후보는 서로를 모르는 Agent가 병렬로 만든다(산출 형태 = 텍스트 스펙, Figma에 쓰지 않는다).
> 비평은 서로의 평가를 모르는 페르소나 Agent가 낸다. 후보를 만든 Agent가 그 후보를 비평하지 않는다.
> **선택은 사람이 한다** — 에이전트가 고르면 "사람의 안목 추출"이라는 목적이 여기서 무너진다.

#### 축: TODO

| 후보 | 스펙 요지 | 페르소나 A 비평 | 페르소나 B 비평 | 선택 |
|---|---|---|---|---|
| 1 | TODO | TODO | TODO | (선택한 후보에만 `✓`) |
| 2 | TODO | TODO | TODO | |

**선택 이유 (사람이 적는다)**: TODO

**남은 트레이드오프**: TODO

### 제작 기록

**레퍼런스 추적**: 승인된 REF/PATTERN ID → 기준 ID → 적용 화면·컴포넌트 노드 ID를 기록한다. 레퍼런스가 없는 사람 직접 요구는 원문 근거를 연결한다. 미승인 패턴은 제작 입력에 넣지 않는다.

| 항목 | 내용 |
|---|---|
| 생성/수정한 노드 ID | TODO (A-4 diff와 rate limit 대응에 쓰인다) |
| 제작 전 스냅샷 | `.cache/nodes-before.json` |
| 제작 후 스냅샷 | `.cache/nodes-after.json` |

### 검증 로그 — 하위 라운드

> 국소 수정 재검과 A단계 재검은 B단계를 거치지 않으므로 라운드가 늘지 않는다.
> **여기에 한 줄씩 추가해 하위 카운트를 올린다.** 이 표가 없으면 그 두 루프는 무한 반복 가능하다.
> 판정 원문을 요약하지 않는다 — 라우팅 1/2 구분이 C단계 서술에 전적으로 의존한다.

### 화면별 C단계 시도 횟수 (출처: feat/yj1 verify-log)

> 상한은 라운드·하위 라운드 외에 **화면 단위**로도 센다. 같은 화면 C 3회 실패 → 사람 에스컬레이션.

| 화면 | C 시도 | 상태 |
|---|---|---|
| (화면 이름) | 0 | — |

| 하위 | 무엇을 했는가 | A단계 판정 원문 | C단계 판정 원문 | 진단 (국소결함 / 결정충돌 / 방향오류 / 반복실패) | 결과 |
|---|---|---|---|---|---|
| 1.0 | 최초 제작 | TODO (A-F·A-T·A-0~A-9 항목별 PASS/FAIL/SKIP/N-A/검증불가 + FAIL 노드 경로 전부 + 축별 소계) | TODO (C-1~C-7·C-X·C-S 판정 + Critic/Advocate/PM + 무엇이 어떻게 보이는지 + 위치) | TODO | TODO |
| 1.1 | TODO (예: C-3 국소 수정) | TODO | TODO | TODO | TODO |

### 기준 자체를 고친 기록 (출처: feat/yj1 · iceberg Miscalibration)

> A단계 위반이 **파일 전반에 일관**되거나 한 항목에 전체 위반의 30% 이상이 몰리면, 산출물이 아니라 기준이 틀린 신호다. **사람 승인 후** 기준을 고치고 여기에 남긴다. 조용히 느슨하게 바꿔 통과시키지 않는다.

| 일시 | 무엇을 (brief YAML 키) | 이전 값 → 새 값 | 왜 (위반 분포 근거) | 승인 |
|---|---|---|---|---|
| | | | | |

### 피드백 원문 로그 — 사람 발화를 행동 전에 원문으로 남긴다

> 2026-09-05 신설. 다른 브랜치에서 디자이너 피드백 13건이 방법론 문장으로 요약된 채 원문·화자·화면·시각이 전부 소실된 사고가 있었다. 그래서 **사람 발화가 디자인이나 기준을 바꾸면 행동 전에** 한 행을 남긴다. 원문 열에는 에이전트 표현 금지 — 해석은 옆 열에. 오버라이드(판정 뒤집기)는 이 표의 특수 사례다.

| FB# | 일시(분) | 화자(역할) | 매체(구두/채팅/Figma 댓글) | 화면 · 렌더/노드 | 원문(따옴표, 요약 금지) | 에이전트 해석(별도) | Agent 판정→사람 판정(뒤집기일 때만) | 증류된 원장 행 / 검증 로그 행 |
|---|---|---|---|---|---|---|---|---|
| FB-01 | TODO | TODO | TODO | TODO | "TODO — 들은 말 그대로" | TODO | — | TODO |

### 오버라이드 로그 — 사람이 Agent 판정을 뒤집은 사건 (피드백 원문 로그의 특수 사례 — FB# 병기)

> 각 행의 "이유 원문"에서 판정을 가른 기준을 증류해 brief.md 「판단기준 원장」에 후보로 보낸다.
> 승격은 사람만 한다. **완화 방향 자동 증류 금지** — 통과가 목적이 되는 순간 검증이 무너진다.

| 하위 | 항목 | Agent 판정 | 사람 판정 | 이유 원문 | 증류된 후보 |
|---|---|---|---|---|---|
| 1.0 | TODO (예: C-4) | TODO | TODO | "TODO — 들은 말 그대로" | TODO |

### V단계 판정 원문

> 실재·흐름 검증(SKILL.md V단계). C 통과 후 1회 이상. 원문 그대로, 요약 금지.

| 하위 | V-1 요구 | V-2 까다로운 경우 | V-3 흐름 연결 | V-4 상태 | V-5 데이터 정합 | V-6 리소스 실재 | 종합 |
|---|---|---|---|---|---|---|---|
| 1.x | TODO | TODO | TODO | TODO | TODO | TODO | TODO |

### 인수인계 GATE 블록

> 단계가 끝날 때마다 하나씩 append. 양식은 `templates/contracts/gate.yaml`. 다음 단계는 `output_refs` 실재와 `target_ref` 일치를 확인한 뒤 시작한다.
> `status`: PASS | PARTIAL(진행 보고, 승인 아님) | FAIL(검사했는데 위반) | BLOCKED(증거·권한·결정 부재로 판정 불가 — 검증 Agent 한도 중단 포함).

```yaml
stage: TODO
mode: TODO                 # plan-only | build | review | improve
project_id: TODO
run_id: TODO
revision: 1.0
producer: TODO
target_ref: TODO           # brief.md target의 file_key + page
input_refs: []
output_refs: []
status: TODO
blockers: []
next_stage: TODO
```

### 확정 잠금 LOCK 블록

> 막(M/W/I/U)이 끝날 때 append. 잠긴 항목은 뒤 막에서 변경 제안 금지 — `UNLOCK 요청`만 가능(사람 승인, 라운드 +1).

```yaml
lock: LOCK-M            # LOCK-M | LOCK-W | LOCK-I | LOCK-U
locked_items: []        # 확정한 것 목록 (예: 무드 어휘 / 화면 목록·순서·CTA 목적지 / 요구 계약 revision)
confirmed_by: TODO      # 역할 (클라이언트 / 디자이너 / 오너)
confirmed_at: TODO
evidence_verbatim: "TODO — 컨펌 발화 원문"
probes: []              # 되묻기 기록 참조 (interview.md 또는 brief 반응 번호)
```

```yaml
unlock_request:         # 잠긴 항목을 바꿔야 할 증거가 나왔을 때만. 에이전트가 조용히 되돌리지 않는다
  target_lock: TODO
  item: TODO
  reason_evidence: TODO
  impact: TODO          # 재작업 막 수·화면 수
  approved_by: null     # 사람 승인 전 null
```

### 라운드 종결

- **최종 C 탈락 항목**: TODO (없으면 `없음 — 통과`)
- **최종 V 탈락 항목**: TODO (없으면 `없음 — 통과` / 미실시면 `미실시`)
- **라우팅 판정**: TODO (`반복 실패` / `방향 오류` / `국소 결함` / `해당 없음`)
- **BLOCKED 사유(있으면)**: TODO — 미해결 결정의 이름으로
- **다음 행동**: TODO

---

<!--
## 라운드 2

새 라운드는 위를 지우지 않고 여기에 라운드 1과 같은 형식으로 덧붙인다.
라운드 3이 C단계에서 탈락한 시점에 사람에게 에스컬레이션하고 멈춘다
(라운드 3을 "시작할 때"가 아니다 — 그러면 실질 상한이 2가 된다).
에이전트가 상한을 스스로 늘리지 않는다.
-->

## 인수인계 메모

> 드라이버 스위치 시 이어 쓴다. 별도 파일을 만들지 않는다.

| 시각 | 누가 → 누구로 | 어디까지 했는가 | 다음은 무엇인가 |
|---|---|---|---|
| TODO | TODO | TODO | TODO |
